# Aruba Session Tracker v0.5.3 사전릴리스

v0.5.3은 읽기 전용 조회, 기존 설정·DB·CSV·Raw 저장 형식과 단순한 화면을
유지하면서 장시간 모니터링의 세션 판정, 시간·복구 경계와 새 HTML 결과의
표시·인쇄 방식을 보강한 안정성 패치입니다.

## 변경 사항

- Source와 Destination이 서로 다른 MD에 있으면 두 MD의 결과를 모두
  확인하고, 이미 활성화된 세션의 MD도 다음 poll에서 계속 조회합니다. 한 MD의
  세션이 확인됐다는 이유만으로 다른 MD의 정상 세션을 `잠시 미확인` 또는
  `종료 확인`으로 바꾸지 않습니다.
- 호스트 키와 전체 MD 검색 승인 창 및 `known_hosts` 잠금도 조회 제한 시간과
  취소를 따릅니다. 제한이 지난 뒤 도착한 승인은 호스트 키 저장이나 전체 MD
  조회에 사용하지 않습니다.
- 외부 USB 또는 UNC 대상이 분리됐을 때 반환되는 대표적인 Windows
  장치·네트워크 단절 오류는 앱 시작을 막지 않고 해당 복구만 보류합니다.
  접근 거부, 네트워크 접근 거부와 자격증명 충돌은 보류로 숨기지 않고 기존처럼
  확인이 필요한 오류로 처리합니다. 외부 경로의 디렉터리가 일반 파일로 바뀐
  경우도 안전하게 중단합니다. 제한 모드에서는 분리된 외부 파일을 건드리지
  않으면서 로컬 기록과 나머지 화면을 사용할 수 있습니다.
- HTML 결과 보고서의 상태는 `확인됨`, `잠시 미확인`, `종료 확인`, `관측됨`
  네 가지로만 표시합니다. 현재 버전이 알 수 없는 새 이벤트는 상태 판정에서
  제외하며, 인식 가능한 이벤트가 하나도 없을 때만 `관측됨`으로 표시합니다.
- 화면에서 접혀 있는 전체 추적 이력도 인쇄할 때는 모두 표시하며, 여러 페이지에
  걸친 표 머리글이 잘리지 않도록 Edge 인쇄 형식을 보완했습니다.
- 실제 주 화면에서 화면 개선 도우미의 상세 창과 시스템 클립보드까지 확인용
  문자열을 전달해, 런타임 IP·계정·암호·Raw·진단 값이 복사되지 않는 경계를
  검증했습니다.

## 호환성과 검증 범위

- 기존에 생성된 파일과 과거 기록은 수정하지 않습니다. SQLite 스키마,
  DB·CSV·Raw 저장 형식, HTML 내보내기 공개 인터페이스는 유지하고 새 HTML
  출력의 표시 구조와 인쇄 스타일만 변경합니다.
- 민감정보가 없는 파서 예제, 가짜 SSH, 임시 SQLite/Raw 저장소, 승인 지연 및
  Windows 오류 주입, UI 검사 문자열, 장시간 가상 조회와 패키지 실행 검사로
  검증합니다.
- 실제 Aruba 장비나 회사 네트워크에는 접속하지 않습니다. 자동 테스트·가상
  장비·패키지 검사 결과는 현장 호환성, 실제 장시간 운용 또는 Authenticode
  서명을 증명하지 않습니다.
- 공개 자산은 코드 서명되지 않은 Windows 11 x64 ZIP 하나입니다. SHA-256은
  릴리스 본문에 추가되고 CycloneDX SBOM은 ZIP 내부에 포함됩니다.

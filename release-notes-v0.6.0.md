# Aruba Session Tracker v0.6.0 continuous 변경 안내

v0.6.0은 기존 읽기 전용 조회와 저장 경계를 유지하면서 데스크톱 운영 화면과
단일 HTML 조사 보고서의 시각 체계를 전면 개편한 이동형 `continuous`
빌드입니다.

## 변경 사항

- 기존 주요 탭 객체를 유지한 상단 NOC 탐색과 운영 헤더에서 MM·MD 설정 수,
  poll 간격, 최근 관측 시각 및 현재 운영 상태를 확인할 수 있습니다.
- 결과 화면에 현재 표시 결과로 확정적으로 계산한 세션 지표와 선택 세션
  요약을 추가하고, 운영 용어와 상세 탭은 한국어로 표시합니다. 기본 표에는
  목적지 포트를 남기고 장비·통계·원문 Flags는 `상세 열 보기`에서 확인하며,
  출발지·목적지 IP와 포트는 헤더에서 정확한 값으로 필터링할 수 있습니다.
- 실행 기록은 출발지 IP → 목적지 IP와 시작 시각을 조합한 표시 ID 및 경과
  시간을 사용하고 내부 UUID를 별도로 유지합니다. CSV·HTML 저장 완료 후에는
  대화상자의 `파일 열기`로 저장 파일을 기본 앱에서 바로 확인할 수 있습니다.
- 알려진 프로토콜/포트 조합만 `443(HTTPS)`처럼 표시하고, 확인되지 않은 포트는
  숫자만 표시합니다. 화면 개선 도우미는 빈 영역 우클릭 또는 Esc로 선택을
  취소할 수 있으며 관련 대화상자도 동일한 어두운 고대비 팔레트를 사용합니다.
- 결과 필터로 선택 행이 숨겨지면 Raw·세션 상세 선택도 함께 지우고, 화면에
  나눠 표시하는 행에도 필터를 즉시 적용합니다. 2,000행 화면 제한에서는 필터가
  검사한 화면 행 수와 전체 저장 결과 수를 구분해 표시합니다.
- 잘못된 입력은 기존 결과 필터와 시작·경과 표시를 변경하지 않으며, 경과 시간은
  시스템 시각 변경에 안전한 monotonic 시계로 계산합니다. 밝은 고대비 모드의
  화면 개선 도우미와 결과 필터 대화상자 대비도 보강했습니다.
- MM·MD 출력의 완료 표식 중복, 완료 뒤 예상하지 않은 데이터 및 비정상적으로 큰
  선언 행 수를 완전한 결과로 채택하지 않고 안전한 파서 진단으로 처리합니다.
  CSV·HTML 저장 뒤 `파일 열기`는 GUI 스레드에서 별도 파일 상태 조회를 하지
  않습니다.
- 일부 회사 Windows PC에서 프로필 저장소·보안 필터가 Python 경로 조회와 열린
  파일 조회에 서로 다른 메타데이터를 반환해 지속 모니터링 시작 또는 결과 저장이
  중단되는 문제를 보강했습니다. 실행 잠금과 Raw 결과 검증은 네이티브 128-bit
  파일 ID를 사용하며, Raw staging 경로도 짧게 유지합니다. 저장 경로 전달 코드는
  `AS86-P/S/R/F`로 실패 단계만 표시하고 경로·IP·계정 정보는 포함하지 않습니다.
- HTML 보고서를 조회 흐름, `조회 대상`, 실행 요약, 전체 저장 이력 기준
  IP·포트/프로토콜 관측 빈도 TOP 5, 최근 결과와 전체 이력 순서의 밝은
  조사 보고서로 재구성합니다. 기존 세션 상태 요약과 이벤트 타임라인은 제거합니다.
- 기존 정확값 필터, 단일 CSP-hash 스크립트, 값 escaping, 외부 리소스 및
  네트워크 접근 금지 계약을 유지합니다.

## 호환성과 검증 범위

- 읽기 전용 명령 allowlist, SSH·parser·수명주기 동작, SQLite schema,
  CSV·Raw 형식과 61개 화면 개선 도우미 ID는 변경하지 않습니다.
- 게시 전 GitHub-hosted Windows x64의 일반 non-soak 검증과 패키지·EXE
  smoke를 요구합니다.
- 이번 작업에서는 fixture-only 20,000 poll 시험을 실행하지 않으며
  `v0.6.0` 태그 또는 변경 불가 버전 릴리스를 만들지 않습니다.
- 실제 Aruba 장비, 회사 네트워크 또는 실제 Windows 11 clean PC에는
  접속하지 않습니다.
- 게시 검증이 성공하면 코드 서명되지 않은 Windows 11 x64 ZIP 하나를 이동형
  `continuous` 사전릴리스로 갱신하며 SHA-256은 검증 후 본문에 추가합니다.

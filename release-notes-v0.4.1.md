# Aruba Session Tracker v0.4.1 사전릴리스

이번 버전은 HTML을 사용 설명서나 진단 문서가 아닌 세션 추적 결과 보고서로
단순화합니다. 프로그램이 실제로 저장한 세션 값과 변화를 한글로 정리하며,
운영 화면의 가독성·고대비 대응도 함께 개선합니다. 기존 설정, SQLite, CSV와
Raw 형식은 그대로 유지합니다.

## 운영 화면 표시 개선

- PySide6 운영 화면에 일관된 색상, 간격, 입력창, 표와 상태 표시 스타일을
  적용해 자주 사용하는 조회 흐름을 더 쉽게 구분할 수 있습니다.
- Windows 고대비 모드를 감지하면 운영체제 색상을 존중하며, 테마 적용 여부와
  주요 대비 조건을 로컬 자동 테스트로 검사합니다.
- 화면 디자인 기준과 세션 조회 화면 명세를 저장소의 디자인 시스템 문서에
  함께 기록했습니다.

## 간결해진 HTML 결과 보고서

- 상단에는 KST 기준 추적 시작·종료 시각, 조회 조건, 전체 관측 수, 고유 세션
  수와 수집 상태만 표시합니다.
- 최신 세션 결과는 마지막으로 확인된 50건을 보여줍니다. 마지막 확인 시각,
  장비명, 프로토콜, 출발지, 목적지와 추적 상태를 한 표에서 확인할 수 있습니다.
- 세션별로 처음·마지막 확인 시각, 패킷·바이트 시작값과 마지막 값·증감,
  처음·마지막 장비명을 표시합니다. 값이 감소해도 원인을 추측하지 않습니다.
- 전체 추적 이력은 기본으로 접어 두지만 선택한 실행에 저장된 모든 관측 행을
  포함합니다. 실시간 화면의 2,000행 제한은 HTML에 적용하지 않습니다.
- 세션 상태는 저장된 수명주기 이벤트만 사용해 `확인됨`, `잠시 미확인`,
  `종료 확인` 또는 `관측됨`으로 표시합니다. 일시적인 통신 실패처럼 종료를
  확인할 수 없는 상황은 세션 종료로 표시하지 않습니다.

## 개인정보와 호환성

- 진단 이벤트와 오류 코드, Raw 본문·경로·해시, CLI, 프로그램 처리 흐름,
  Troubleshooting, Warning, 개발자 정보, 자격증명과 로그는 HTML에서
  제외합니다.
- 내부 IP와 장비명은 추적 결과이므로 보고서에 포함될 수 있습니다. 외부에
  공유하기 전에는 파일을 열어 민감한 세션 정보를 직접 제거하고 확인하십시오.
- 보고서는 외부 리소스나 JavaScript를 사용하지 않는 단일 HTML5 파일이며,
  저장된 문자열을 HTML 이스케이프합니다.
- 이번 보고서 변경은 HTML 표현만 바꾸며 기존 설정, SQLite 스키마, CSV와
  Raw 형식, 과거 실행 기록과 내보내기 인터페이스는 바꾸지 않습니다.

## 검증 범위

- 결과 없음, 단일·다중 세션, 값 증가·변화 없음·감소, 장비 변경,
  `MISSED`·`CLOSED`, KST 날짜 경계와 2,005건 이상의 이력을 비식별 fixture와
  로컬 가짜 구현으로 검사합니다.
- HTML 주입 방지, 자격증명·Raw·진단 정보 미포함, 결정적 출력, 원자적
  내보내기와 강제 종료 복구를 자동 테스트로 확인합니다.
- offscreen Qt와 Windows x64 패키지 검증은 실제 Windows 화면의 육안
  확인이나 실제 Aruba MM/MD 접속을 대신하지 않습니다.
- 실제 Aruba 장비와 회사 네트워크에는 접속하지 않았습니다. 현장에서 발견한
  문제는 민감정보를 제거한 증상과 오류 코드를 바탕으로 별도 수정합니다.
- 공개 자산은 unsigned Windows 11 x64 ZIP 하나입니다. SHA-256은 릴리스
  본문에 생성 시 추가되고 CycloneDX SBOM은 ZIP 내부에 포함됩니다.

# 변경 이력

## Unreleased

- 조회 결과 표의 출발지·목적지 IP와 포트를 정확한 값으로 선택해 필터링할 수
  있도록 보강하고, 기본 화면에는 목적지 포트를 남기면서 장비·통계·원문 Flags는
  `상세 열 보기`에서 확인하도록 정리. 알려진 프로토콜/포트 조합만
  `443(HTTPS)`처럼 표시하고 알 수 없는 포트는 숫자 그대로 유지.
- 실행 기록에 출발지 IP → 목적지 IP 기반 표시 ID와 시작·종료 시각, 경과 시간을
  추가하되 내부 UUID와 CSV 저장 경계는 유지. CSV·HTML 저장 완료 대화상자에서
  저장된 파일을 기본 앱으로 바로 열 수 있는 선택지를 제공.
- 세션 지표·상세 탭·화면 개선 도우미의 운영 용어를 한국어로 통일하고,
  우클릭/Esc 빈 영역 선택 취소와 어두운 고대비 대화상자 스타일을 보강.

- 1080×680 최소 창에서 고급 조건과 상세 정보를 함께 펼쳐도 결과표의 첫 행,
  네 개 세션 지표와 상세 탭이 잘리지 않도록 중복 안내를 접고 지표 카드의
  높이와 여백을 압축. 숨긴 조회 범위와 관측 상태의 의미는 결과표 접근성
  설명에 유지하고, 연속된 빈 조회에서도 최신 MM·MD 범위로 즉시 갱신.
- Windows CI의 SSH deadline 검증을 가상 시계로 전환해 최초 host-key 저장
  속도와 분리하고, 배율별 UI smoke가 비동기 종료 완료까지 확인하도록 보강.
  느린 hosted runner의 cold start는 단계별 출력과 제한된 45초 상한으로
  진단하며 실제 런타임 deadline 정책은 변경하지 않음.
- 20,000회 fixture 장시간 검증이 poll마다 SSH를 다시 연결하던 과거 동작을
  요구하지 않고, 현재 모니터링의 MM·MD 연결 재사용 계약을 직접 확인하도록
  수정.
- 동일 Windows 이미지에서도 hosted runner 저장장치 편차로 두 20,000회
  worker가 각각 기존 3,200초 상한에 도달한 증거를 반영해 내부 상한을
  4,000초, 외부 workflow 감시를 150분으로 제한적으로 조정. poll 수와
  SQLite 내구성·자원 누수 판정 기준, 수동 실행 전용 정책은 그대로 유지.
- 동일 MD의 datapath 출력에서 같은 controller·5-tuple 키가 두 번 나타나면
  임의의 첫 행만 완전한 결과로 채택하지 않고 기존 `MD_PARSE / PARSE_PARTIAL`
  (`AS32`)로 안전하게 중단. Raw 원문은 보존하고 중복 행을 근거로 한 세션
  수명주기 변경과 미관측 종료 판정은 보류하며, 저장 schema와 controller 간
  중복 흐름 계약은 유지.
- SQLite 연결 생성 직후부터 설정 PRAGMA와 실제 사용 구간 전체를
  `try/finally`로 보호해 초기 설정 오류에서도 연결을 닫고, 지원 코드 테스트의
  직접 연결도 명시적으로 종료하도록 수정.
- 로컬 coverage 기본 하한을 실제 릴리스 정책과 같은 83%로 통일. PR의 Windows
  패키지 빌드·검증은 유지하면서 소비되지 않는 CI artifact 업로드와
  Continuous가 이미 반복하는 `main` push CI만 제거하고 수동 실행은 유지.

## 0.6.0 - 2026-09-01

- 기존 운영 위젯과 61개 화면 개선 도우미 ID를 유지하면서 데스크톱 화면을
  다크 NOC 운영 콘솔로 재구성. 상단 운영 헤더와 탐색, 확정적으로 계산한
  세션 지표, 고밀도 결과표 및 `DETAILS`·`RAW CLI`·`DIAGNOSTICS` 구조를 추가.
- 설정값과 현재 표시 결과로 확인되는 정보만 화면에 표시하고, MM/MD 상태,
  통신 성공, 장애 원인 또는 네트워크 경로는 추정하지 않음.
- 단일 HTML을 밝은 조사 보고서 형식으로 개편. 조회 흐름과 실행 요약,
  최신 표시 세션에 범위를 한정한 상태 요약과 저장 사실 타임라인, 최근 결과와
  전체 관측 이력을 분리해 표시.
- HTML의 정확값 필터, 정확한 CSP hash, 값 escaping, 외부 리소스·네트워크
  접근 금지 및 전체 관측 보존 경계를 유지.
- 읽기 전용 명령, SSH·parser·수명주기 동작, SQLite schema와 CSV·Raw 형식은
  변경하지 않음. 실제 장비와 장시간 안정성 시험은 이번 검증 범위에 포함하지
  않으며 변경 불가 `v0.6.0` 태그를 만들지 않음.

## 0.5.9 - 2026-09-01

- 지속 모니터링 동안 장비별 SSH 연결을 poll 사이에 재사용하고, 사용자가
  중지하거나 연결이 끊기거나 장비 설정·실행 자격증명·네트워크가 바뀐
  경우에만 기존 연결을 닫고 다시 연결하도록 변경. 시간 또는 poll 횟수에
  따른 주기적 재인증은 하지 않으며 `AUTH_FAILED`는 계속 자동 재시도하지 않는
  확인 필요 오류로 처리.
- 진단 목록과 오류창에 민감정보가 없는 `ASxx` 전달 코드를 함께 표시하고 실행
  이력의 `최근 전달 코드` 열에 남겨, 원문이나 자격증명 없이 현재 안내와 이력을
  연결해 확인할 수 있도록 보완. 기존 다섯 운영 상태 값은 유지.
- 결과 표의 의미를 `관측 상태`와 `세션 특이사항`으로 분리. 수명주기 상태와
  플래그 기반 주의 정보를 한 열에 섞지 않고, Raw Flags 원문은 상세 정보에서만
  확인하도록 화면 표현을 정리.
- 일반 `validate.ps1`, CI와 `continuous` 검증은 `pytest -m 'not soak'`만
  실행. fixture-only 20,000 poll soak는 자동 예약을 제거한 수동 nightly와
  버전 릴리스의 별도 exact-tag job으로 격리.
- SQLite schema와 CSV·HTML·Raw 저장 형식은 변경하지 않음. 실제 Aruba 장비나
  회사 네트워크에 접속하지 않았으며 실제 환경 또는 장시간 운용 검증 결과를
  주장하지 않음.

## 0.5.8 - 2026-09-01

- `show datapath session table <IP>` 출력에 `Entries: N` footer가 없을 때도
  마지막 비어 있지 않은 줄이 AOS 8의 정상 enable 프롬프트이면 완료된 출력으로
  인정. 미저장 설정 `^`, crash 정보 `*`, 결합 상태 `^*`, 길이·문자 집합을
  제한한 bare-host 프롬프트를 지원해 정상 MD 결과가 `MD_PARSE / PARSE_PARTIAL`로
  폐기되던 false negative를 수정.
- 프롬프트는 출력 마지막 줄 전체가 허용 형식과 일치할 때만 완료 표식으로 사용.
  단독 상태 표식, 임의 표식, 잘못된 순서 `*^`, 중복 표식, config-mode
  프롬프트와 후행 텍스트는 계속 거부하고, 기존 행 스키마·행 수·출력 한도
  검증은 유지.
- 오프라인 tech-support의 명령 echo와 최종 프롬프트에도 같은 AOS 상태 표식
  계약을 적용하고, Netmiko의 prompt read timeout을 민감한 원문 없이 재시도
  가능한 수집 오류로 변환.
- 실제 장비에는 접속하지 않음. 비식별 fixture와 로컬 parser/orchestration
  검증, GitHub-hosted Windows x64 전체 CI와 fixture-only 20,000 poll soak,
  패키지·EXE smoke를 사전릴리스 게시 전 수행할 검증 범위로 제한.

## 0.5.7 - 2026-09-01

- 주 탭 오른쪽에 제품명, 버전, 로컬 전용·읽기 전용 경계를 표시하는 운영
  헤더를 추가하고, 출발지·목적지를 분리한 세션 흐름 카드와 현재 양방향 설정을
  보여 주는 방향 표시, 결과 제목·안내·빈 상태와 실행 상태 막대를 추가.
- 결과표와 Raw·진단 패널을 기본 크기에서는 위아래로 배치하고, 창 높이가
  760 logical pixel 미만이거나 고급 조건을 펼친 경우에는 좌우로 전환해
  1080×680 및 100%·125%·150% 배율에서도 결과표가 접히지 않도록 보완.
- 기록 화면에서 실제 선택 행이 있을 때만 CSV·HTML·선택 삭제를 활성화하고,
  기록 없음·불러오는 중·읽기 실패를 서로 다른 안내로 표시. 장비 설정의
  자격증명 비저장 안내는 좁은 모서리 대신 전체 폭으로 배치.
- 탭, 결과표, Raw·진단, 장비 필드, 기록·내보내기 동작의 accessible name과
  키보드 focus 경계를 보강. 기존 위젯, 61개 Developer Inspector ID, 결과표
  15열 순서, 명령·SSH·파서·저장·내보내기 형식은 변경하지 않음.
- 단일 HTML 보고서의 필터를 보조 표면으로 구분하고 최신·전체 건수를 각 결과
  제목 옆에 배치. 활성 필터에 맞춰 최신 결과 설명과 빈 상태를 갱신하고,
  361~520 px 두 열·360 px 이하 한 열 배치와 상태 배지 경계를 보강하면서
  단일 CSP-hash 스크립트와 외부 리소스 금지 계약을 유지.
- 실제 장비에는 접속하지 않음. GitHub-hosted Windows x64 Python 3.13 전체
  검증과 Edge 보고서 상호작용, fixture-only 20,000 poll soak, 패키지·EXE
  smoke는 사전릴리스 게시 전 수행할 검증 범위이며 이 변경 이력 작성 시점에는
  결과를 확정하지 않음.

## 0.5.6 - 2026-09-01

- 활성 Primary/Standby MM의 동일 주소·포트, 활성 MD의 중복 주소·정규화 이름과
  Current switch 별칭 충돌을 `AppConfig` 생성 단계에서 차단. 꺼 둔 장비
  placeholder는 기존 구성 호환성을 위해 충돌 검사에서 제외.
- SQLite schema를 v3로 올리고 각 poll의 ID, 실행 ID와 payload SHA-256을
  `poll_commits` durable receipt로 같은 transaction의 마지막에 기록. 동일 poll
  ID와 동일 내용 재시도는 이미 commit된 결과를 반환하고 관측·진단·수명주기
  이벤트와 Raw 연결을 중복 삽입하지 않으며, 다른 실행이나 내용의 ID 재사용은
  거부.
- DB commit 뒤 manifest·staging·lease 정리에 실패한 경우 저장 실패로
  잘못 처리하지 않고 `COMMITTED_RECOVERY_PENDING`으로 반환. 같은 poll ID
  재시도와 다음 시작 복구가 receipt 및 Raw 무결성을 대조해 남은 정리를 완료.
  불확정 poll 재확인 중 추가 오류나 동시 중지가 발생해도 준비 상태와 ID를
  보존해 새 poll로 중복 저장하지 않고, 확인된 commit만 모니터 상태에 반영.
  실패했던 실행 종료 상태의 동시 재시도도 직렬화해 완료된 run을 다시 보류하지
  않도록 함.
- SQLite `BUSY`와 `LOCKED`에만 제한된 재시도를 적용하고 commit 응답이 불명확한
  경우 receipt를 다시 확인. receipt까지 읽을 수 없는 상태는 중복 저장을
  추측하지 않고 동일 poll ID 복구가 필요한 불확정 오류로 중단.
- 시작 시 delete manifest를 Raw batch manifest보다 먼저 복구해 삭제 staging으로
  잠시 이동한 정상 Raw를 누락·손상으로 오판하지 않도록 순서를 고정하고,
  lease 정리 실패 시에도 manifest를 복구 anchor로 유지.
- 실제 장비에는 접속하지 않음. GitHub-hosted Windows x64 Python 3.13 전체
  검증과 fixture-only 20,000 poll soak, 패키지·EXE smoke는 사전릴리스 게시 전
  수행할 검증 범위이며 이 변경 이력 작성 시점에는 결과를 확정하지 않음.

## 0.5.5 - 2026-09-01

- AOS 8 `show global-user-table list ip`가 열 이름을 가운데 정렬해 출력하는
  형식에서도 대시 구분선의 실제 시작 위치로 IP, MAC, Name, Current switch,
  Role, Auth와 AP name을 분리하도록 수정. 빈 Name/Auth 필드가 있어도
  Current switch를 정확히 찾으며, 출발지·목적지 단독 또는 동시 조회에서
  동일하게 적용.
- 열 구분선과 헤더가 일치하지 않거나 헤더 연속 줄이 역순·중복인 출력은
  기존처럼 `PARSE_PARTIAL`로 안전 중단하고 MD 조회를 시작하지 않음. 구형
  좌측 정렬 헤더와 단일 연속 구분선 형식은 제한된 호환 경로로 유지.
- 실제 장비에는 접속하지 않고 HPE 문서 형태를 비식별 문서용 주소로 재현한
  fixture와 GitHub-hosted Windows x64 검증으로 범위를 제한.

## 0.5.4 - 2026-08-31

- 출발지 IP와 목적지 IP를 각각 선택 입력으로 바꾸고 둘 중 하나만 입력해도
  해당 주소가 포함된 세션을 안전한 필터형 명령으로 조회하도록 개선. 두 주소가
  모두 비어 있으면 조회를 차단하며, 양방향 검색은 단일 주소가 출발지 또는
  목적지인 흐름에 동일한 포트 교환 규칙을 적용.

- 세션 조건과 실행 중에만 쓰는 로그인 정보를 한 줄에 재배치해 결과표의
  세로 공간을 넓히고, Source/Destination/Controller 등 혼합 표기를
  출발지·목적지·장비 중심 한글 표시명으로 정리. 기존 필드·열 순서·저장값과
  61개 Inspector ID는 그대로 유지.
- 주 탭을 짙은 네트워크 운영 콘솔 막대로 정리하고 다섯 운영 상태에
  presentation-only 역할을 부여. 상태 의미는 기존 한글 텍스트로 항상 남기며
  Windows 고대비에서는 시스템 팔레트를 계속 사용.
- 기록 화면의 내보내기와 삭제 동작을 양쪽으로 분리하고 저장 상태 코드를
  `완료`, `일부 결과`, `실패`, `취소` 등으로 표시. 원래 DB 상태 코드는
  변경하지 않고 셀 도움말에 유지.
- HTML 상단의 프로토콜·장비 분포를 제거하고 compact masthead·조회 요약과
  IP·프로토콜·포트 우선 표로 재배치. IPv6는 `[주소]:포트`로 구분하고
  자동완성·CSP·최신 50개·전체 이력·인쇄 계약은 유지.
- Windows 패키지 빌드 PATH를 격리해 빌드 PC의 OpenSSL DLL 혼입을 차단하고,
  모든 EXE·DLL·PYD를 대소문자 비구분 exact allowlist와 SHA-256 manifest,
  CycloneDX SBOM에 연결. Qt Schannel 실제 활성화, 정적 OpenSSL·LibYAML·
  libsodium 참조, pyserial 3.5 공식 BSD 원문을 검증하고 미분류·변조·금지
  native 파일을 거부. 관련 라이선스와 LGPL 소스 요청·재빌드 안내는 ZIP
  내부에 포함하되 향후 요청 처리 자체는 자동 테스트가 증명하지 않음을 명시.
- 느린 GitHub-hosted Windows 저장소에서도 기본 2,000회 fixture soak가 잘못
  시간 초과되지 않도록 하위 프로세스 하한을 900초로 조정. 무한 대기 차단과
  20,000회 상한 3,200초, 외부 120분 watchdog 계약은 그대로 유지.

- 승인된 `known_hosts` 항목이 있는 장비는 별도 호스트 키 probe를 반복하지
  않고 strict SSH 연결 한 번으로 키를 검증하도록 변경. 최초 미등록 장비의
  지문 확인·승인은 유지하며, 감싸진 host-key 불일치는 재시도 가능한 통신
  장애가 아닌 `HOST_KEY_CHANGED`로 중단.
- 동일한 5-tuple이 여러 MD에서 동시에 관측되면 Controller별 관측과 Raw 연결을
  모두 보존하고 다음 조회에도 해당 MD 전체를 포함. 중첩 중에는 거짓
  `CONTROLLER_CHANGED`, `MISS`, `CLOSED`를 만들지 않고 권위 있는 단일 MD로
  해소된 경우에만 실제 이동을 기록.
- 장비의 `Permission denied`, `Insufficient privileges`, `Authorization failed`,
  `Access denied` 응답을 `COMMAND_REJECTED`로 분류하고 반복 재시도 대신 확인이
  필요한 권한 오류로 처리.
- 단발 조회는 권위 있는 정상 빈 결과를 `COMPLETED`, 양성 관측이 남은 비권위
  결과를 `PARTIAL`, 관측이 전혀 없는 비권위 기술 실패를 `FAILED`로 구분.
- `기록 및 내보내기` 화면에 Raw 파일 수·Raw 용량·전체 관리 데이터·여유 공간을
  읽기 전용으로 표시하고 Raw 100,000개부터 비차단 주의를 안내. 조회 중단,
  반복 팝업, 자동 압축·삭제와 저장 형식 변경은 추가하지 않음.
- HTML 결과 요약 아래에 IP·프로토콜·포트 필터와 전체 초기화를 추가. IP와
  포트 일부를 입력하면 전체 추적 이력의 실제 완성값을 최대 12개 추천하고,
  추천값 선택 또는 정확한 완성값 입력 후 Enter에서만 필터를 적용.
- 추천값에 출발지·목적지·양쪽 위치를 표시하고, IP·프로토콜·포트의 정확한
  일치 조건을 AND로 결합. 최신 결과와 전체 이력의 표시/전체 건수를 각각
  안내하며 최신 50건 밖에만 일치 결과가 있는 경우도 구분.
- 화면과 인쇄에 현재 필터를 함께 적용하되, 필터링은 행 표시만 변경하고
  저장된 모든 관측 행을 단일 HTML 안에 그대로 보존. 필터는 파일·URL이나
  브라우저 저장소에 기록하지 않고 보고서를 다시 열 때 초기화.
- 전체 이력의 IP·포트·프로토콜을 처음 열 때 한 번 역색인하고, 입력이 바뀌면
  이전 추천과 ARIA 선택을 즉시 폐기해 지연된 과거 추천이 적용되지 않도록 함.
- 외부 리소스와 외부 JavaScript는 계속 차단하고, 정확한 SHA-256 CSP 해시로
  허용한 단 하나의 로컬 필터 스크립트만 사용. 인터넷 통신, 외부 라이브러리,
  저장소, 클립보드, `eval`을 사용하지 않으며 CSP 차단 시 전체 정적 보고서를
  그대로 표시.
- 기존 `RunReportSnapshot`, HTML 렌더링·원자적 쓰기·스트리밍 내보내기와
  `SessionStore.export_run_html` 인터페이스, SQLite·CSV·Raw 형식을 유지.
- 실제 Aruba 장비에는 접속하지 않고 비식별 fixture, 대용량 이력, 브라우저와
  Windows x64 패키지 검증으로 범위를 제한.

## 0.5.3 - 2026-08-30

- Source와 Destination이 서로 다른 MD에 있을 때 두 MD와 기존 활성 세션의
  MD를 계속 확인해, 한 MD에서 세션이 확인됐다는 이유만으로 다른 MD의 정상
  세션이 잘못 `MISS/CLOSED` 처리되지 않도록 보완.
- 호스트 키와 전체 MD 검색 승인 대기를 조회 제한 시간과 취소 처리 범위에
  포함하고, 제한 시간이 지난 뒤 도착한 승인으로 known_hosts를 저장하거나
  MD 조회를 시작하지 않도록 강화. known_hosts의 프로세스 내부 잠금과
  Windows 파일 잠금 경합에도 같은 제한을 적용.
- 외부 USB·UNC 복구에서 Windows의 장치 분리·네트워크 단절 오류를 추가로
  복구 보류 처리하되 접근 거부·네트워크 접근 거부·자격증명 충돌은 계속
  확인이 필요한 오류로 중단.
- HTML 보고서의 수명주기 상태를 `확인됨`, `잠시 미확인`, `종료 확인`,
  `관측됨` 네 가지로 제한하고 현재 버전이 알 수 없는 새 이벤트가 저장된 정상
  상태를 덮어쓰지 않도록 수정.
- 화면에서 접힌 전체 추적 이력도 Edge 인쇄에는 모두 포함되도록 HTML 구조와
  인쇄 스타일을 수정하고 반복 표 머리글의 페이지 경계 잘림을 방지.
- 실제 주 화면의 상세 창과 시스템 클립보드까지 확인용 문자열을 전달하는
  시험을 추가해 화면 개선 도우미가 런타임 비밀정보를 복사하지 않는지 검증.

## 0.5.2 - 2026-08-30

- 외부 USB 드라이브가 분리되거나 UNC 공유를 사용할 수 없어도 앱은
  정상적으로 시작하고 해당 외부 복구 건만 보류한다고 안내하도록 개선.
- CSV와 HTML 각각 `PREPARED`, `RENDERED`, `INSTALLED`,
  `DB_RECEIPT_COMMITTED`, `DB_COMMITTED`에서 저장 위치가 분리되는 10개
  조합을 회귀 시험해 재연결 뒤 단계별 복구 결과를 검증.
- 버전 릴리스가 exact tag commit에서 `ARUBA_SOAK_POLLS=20000`인 fixture-only
  장시간 시험을 별도 120분 제한 job으로 반복하고, 패키지 build와 이 시험이
  모두 성공한 뒤에만 게시되도록 자동화와 회귀 검사를 보강.
- 이동형 `continuous` 사전릴리스의 별도 게시 runner에도 고정 runtime lock과
  `pip check`를 적용해 검증 도구가 `paramiko` 누락으로 중단되지 않도록 수정.

## 0.5.1 - 2026-08-30

- Versioned release 게시 작업에도 고정 runtime 검증 의존성을 설치하고
  `pip check`를 수행해, 검증된 ZIP 생성 후 게시 전 패키지 재검증이
  `paramiko` 누락으로 중단되지 않도록 수정.
- Windows 타이머가 조회 마감 직전에 일찍 깨어나더라도 SSH 감시기가 실제
  단조 시계의 마감 시각을 다시 확인하도록 보완.
- 창 종료를 위해 예약한 Qt 콜백을 창 수명에 연결하고 daemon 작업의 늦은
  완료 신호가 Qt 종료와 겹쳐도 처리되지 않은 예외를 남기지 않도록 보완.
- 백그라운드 저장소 대조와 상태 확인이 겹쳐 SQLite WAL이 정상 삭제되는
  순간을 `DB_WRITE_FAILED`로 오인하지 않도록 파일 상태 확인을 단일화.
- 두 가상 soak의 유한 제한 시간을 20,000회 야간 범위와 workflow 전체 제한에
  맞추고, 느린 GitHub Windows 임시 디스크에서도 정상 작업을 오판하지 않도록 보완.

## 0.5.0 - 2026-08-30

- HTML 결과 보고서에서 패킷·바이트와 증감 영역을 제거하고, 최신 세션 결과와
  전체 추적 이력을 프로토콜 및 출발지·목적지 `IP:포트` 중심으로 단순화.
- 패킷·바이트 원본은 SQLite, CSV와 Raw에 계속 보존하며 기존 내보내기
  인터페이스와 저장 형식을 변경하지 않음.
- 비식별 Aruba tech-support 텍스트를 제한된 크기로 읽는 오프라인 파서와
  프로토콜·포트·Flags·ToS의 추측 없는 분석 코어를 추가하고 live
  `SessionObservation`/DB에 자동 합치지 않는 안전 경계를 유지.
- SSH 연결 수립·prompt·enable·명령 수신에 4 MiB·50,000행 상한을 적용하고,
  전체 poll 300초 deadline 또는 취소 시 socket-first abort로 blocked read를
  깨운 뒤 caller 경로에서 transport를 정리하도록 강화.
- Primary/Standby 및 여러 MD에 남은 poll 시간을 공정하게 배분하고 전수조회
  시작 MD를 순환하며, 활성 흐름 20,000개 포화 시 비권위 결과로 처리해
  잘못된 `MISS/CLOSED`가 발생하지 않도록 보완. 같은 poll에서 합법적으로
  여러 상태 전환이 발생해도 별도 수명주기 이벤트 상한 안에서 저장을 반복
  실패하지 않도록 관측 상한과 이벤트 상한을 분리.
- 매 poll 전체 Raw 32 MiB·관측 20,000건 상한을 수집기와 저장소 양쪽에서
  검증하고, 여러 command Raw를 section 길이·SHA-256이 포함된 단일 poll
  bundle로 저장해 장시간 실행의 파일 수 증가를 완화.
- Raw/DB poll 기록, 관리·외부 CSV/HTML 내보내기와 삭제를 manifest 기반으로
  복구하며 파일 hash·fsync·이동을 SQLite writer transaction 밖에서 끝내
  모니터링 기록과의 잠금 경합을 줄임.
- Windows 8.3 짧은 경로와 긴 경로가 섞여도 디렉터리 identity로 같은 관리
  루트를 확인하고, 양방향 별칭 아래의 junction/reparse 구성 요소는 계속
  거부하도록 보완. 종료 시 lease handle을 먼저 닫고 일시적 공유 위반은
  제한적으로 재시도하며 불완전 종료 경로는 worker lock을 기다리지 않음.
- HTML/CSV 행을 cursor로 스트리밍하고 Raw 무결성 hash에 chunk별 취소·진행
  확인을 추가해 대용량 보고서가 GUI 종료와 WAL 정리를 장시간 막지 않도록 함.
- 저장소 상태의 빠른 DB 집계와 명시적 background 파일 대조를 분리하고,
  외부 드라이브 부재로 미뤄진 보고서 복구는 기록 새로 고침에서 재시도하며
  남은 건수를 민감 경로 없이 안내.
- 조회·기록·내보내기·삭제를 daemon 작업으로 분리하고 15초 종료 grace 후
  다음 실행 복구로 전환하며, 시작 초기화는 반응형 화면과 30초 grace를 사용.
- 사용자 세션당 단일 실행 mutex, 절전 복귀·네트워크 변경 감지, MM 위치
  cache 무효화와 실행 중 자격증명 변경 감지를 추가.
- 예외 메시지·traceback·경로·운영 데이터를 제외한 1 MiB 순환 crash journal과
  비밀정보를 보존하지 않는 monitor failure DTO를 추가.
- GitHub Actions를 검증한 immutable Node 24 action commit으로 갱신하고,
  명령 생성→가짜 SSH→실제 파서→수명주기→SQLite/Raw 전체 파이프라인
  2,000회 PR soak 및 최대 50,000회 야간 경로를 추가.
- Windows 패키지 검증에서 Python 없는 PATH, 한국어 LocalAppData/보고서 경로,
  loopback fake SSH 정상·인증 실패, 실제 MM/MD 명령·파서·SQLite 저장을 확인.
- 실제 Aruba 장비와 회사 네트워크에는 접속하지 않았으며 자동·가상·패키지
  검증을 현장 호환성 증거로 확대 해석하지 않음.

## 0.4.1 - 2026-08-30

- PySide6 운영 화면에 일관된 운영 콘솔 테마를 적용하고 Windows 고대비 모드를
  존중하도록 개선했으며, 색상 대비와 테마 적용을 자동 테스트로 검증.
- 화면 디자인 기준과 세션 조회 화면 명세를 저장소의 디자인 시스템 문서로
  추가해 구현 기준을 명확히 기록.
- HTML을 사용 설명서나 진단 문서가 아닌 세션 추적 결과 보고서로 단순화.
- 보고서 상단에 KST 기준 추적 시간, 조회 조건, 전체 관측 수, 고유 세션 수와
  수집 상태를 표시하고 최신 세션 결과는 50건으로 제한.
- 세션별 첫 확인값과 마지막 확인값을 바탕으로 패킷·바이트 변화와 장비
  변경을 실제 저장값 그대로 표시하며, 감소 원인은 추측하지 않음.
- 전체 추적 이력은 기본으로 접어 두되 SQLite에 저장된 모든 관측 행을
  빠짐없이 포함하고 화면의 2,000행 제한은 적용하지 않음.
- HTML에서 진단 이벤트, 오류 코드, Raw 본문·경로·해시, CLI, 프로그램 흐름,
  Troubleshooting과 개발자 정보를 제외하고 자격증명·로그 미포함 원칙 유지.
- 이번 보고서 변경은 HTML 렌더링만 바꾸며 기존 설정, SQLite 스키마, CSV와
  Raw 저장 형식, 내보내기 인터페이스와 과거 실행 기록의 호환성 유지.
- 실제 Aruba 장비에는 접속하지 않고 비식별 fixture, 로컬 가짜 구현, 임시
  저장소, 대용량 이력과 Windows 패키지 검증으로 릴리스 범위를 제한.

## 0.4.0 - 2026-08-29

- 자주 쓰는 자격증명과 Source/Destination만 기본 표시하고
  `지속 모니터링 시작`·`현재 조회`, `고급 조건 보기/숨기기`,
  `상세 정보 보기/숨기기`로 입력과 결과를 점진적으로 펼치는 화면 제공.
- 운영 상태를 `대기·조회 중·정상·재시도 중·확인 필요` 다섯 값으로 제한.
- F12 UI Inspector를 한글 중심 `화면 개선 도우미`로 정리하고 기존 60개
  고정 ID를 유지한 채 상세 열 전환 ID를 추가해 총 61개, 세션 조회 24개
  카탈로그로 갱신.
- Primary MM 실패 뒤 Standby 성공을 복구된 경고로 처리하고 일시 MM/MD
  장애에 제한된 backoff를 적용해 비권위 실패 중 false CLOSED를 방지.
- 전체 MD 조회 빈도, poll 시간·Raw·관측 상한과 활성 SSH 취소 경로를 추가.
- 저장 공간 상태, 단계형 내보내기 복구, 삭제 미리보기 폐기·상한과 날짜·시간
  Raw 분산 저장을 추가하면서 기존 DB·보고서·Raw 경로 호환성 유지.
- 매 poll SSH 전에 빠른 1 GiB hard-stop 검사를 수행하고 전체 저장소 통계는
  60초 간격으로 제한했으며, UI 2,000행과 무관하게 HTML·CSV·SQLite에 전체
  관측 결과를 유지.
- 기록·내보내기·삭제와 대량 결과 렌더링이 GUI 이벤트 처리를 장시간 막지
  않도록 갱신 시점과 작업 수명주기를 분리.
- GitHub 공개 자산을 Windows x64 ZIP 하나로 단순화하고 SHA-256은 릴리스
  본문에, CycloneDX SBOM은 ZIP 내부에 제공.
- `continuous` 초안을 릴리스 ID로 재개하고 빈 중복 초안만 안전하게 정리하며,
  실패 시 이전 워크플로 커밋으로 되돌리지 않는 전진 복구 방식으로 보완.

## 0.3.1 - 2026-08-28

- 조회·모니터링·CSV·HTML·F12 Inspector의 기존 기능과 형식을 유지하면서
  실행 소유권, 취소, 종료 저장과 Qt 비동기 작업 수명주기를 강화.
- 한 번의 조회 결과, Raw 파일, 관측·진단·수명주기 이벤트를 복구 가능한
  단일 batch로 기록하고 프로세스 간 실행 lease로 동시 기록 충돌을 차단.
- Raw와 관리 내보내기에 작업 manifest, 무결성 확인, 장애 복구를 적용하고
  삭제 미리보기의 파일 SHA-256을 재확인하도록 강화.
- 설정, known_hosts, DB, Raw, 내보내기 관리 경로의 link/reparse point와
  실행 중 디렉터리 교체를 차단하고 설정·known_hosts 읽기 크기를 제한.
- CSV를 일정 크기 묶음으로 읽고 Raw 해시를 청크 단위로 계산해 긴 실행의
  메모리 사용량을 제한.
- 전역 line coverage 83%와 핵심 모듈 branch coverage 65%를 CI 정책으로
  추가하고 10,000회 모니터 fixture 및 500회 Qt 수명주기 soak를 검증.
- 릴리스 draft, GitHub 자산 digest, 인증·공개 재다운로드와 이동형
  `continuous` 실패 복구 절차를 강화.

## 0.3.0 - 2026-08-28

- 평소에는 숨겨져 있고 수정키 없는 `F12`로만 켜고 끄는 UI Inspector 추가.
- 세션 조회, 장비 설정, 기록 및 내보내기 화면의 운영 요소를 정적 한국어
  이름, 고정 ID, 화면 경로, 저장소 상대 소스 경로와 용도로 식별.
- 요소 선택 중인 클릭을 실제 동작에 전달하지 않고 `Esc`로 선택만 취소하는
  안전한 점검 흐름 제공.
- 클립보드 복사를 정적 UI 메타데이터와 비어 있는 현상·변경 요청 입력란으로
  제한하고 런타임 네트워크·자격증명·장비·표·Raw·로그·설정 값은 읽지 않음.
- Inspector 활성 상태를 CLI, 환경변수, 설정, 레지스트리 또는 SQLite에
  저장하지 않으며 외부 전송 기능을 제공하지 않음.

## 0.2.0 - 2026-08-28

- 기존 CSV 내보내기를 유지하면서 실행별 독립형 HTML5 결과 보고서 추가.
- 반응형 Summary, 조회 흐름, 세션/Flags, 수명주기, 진단, 수집 증거,
  Troubleshooting, Warning과 Quick Reference 제공.
- 외부 리소스 차단, 저장 데이터 HTML 이스케이프, Raw 본문 제외와
  생성 보고서의 Git/릴리스 패키지 유입 차단.

## 0.1.0 - 2026-08-28

- Windows 11용 한국어 Aruba Session Tracker 최초 사전릴리스.
- MM Current switch 탐색과 해당 MD의 필터형 datapath 세션 조회.
- 1회 조회, 지속 모니터링, SQLite/Raw/CSV 기록, 엄격한 SSH 호스트 키 검증.

# 변경 이력

## Unreleased

- 이동형 `continuous` 사전릴리스의 별도 게시 runner에도 고정 runtime lock과
  `pip check`를 적용해 검증 도구가 `paramiko` 누락으로 중단되지 않도록 수정.

## 0.5.1 - 2026-08-30

- Versioned release 게시 작업에도 고정 runtime 검증 의존성을 설치하고
  `pip check`를 수행해, 검증된 ZIP 생성 후 게시 전 패키지 재검증이
  `paramiko` 누락으로 중단되지 않도록 수정.
- Windows 타이머가 조회 마감 직전에 일찍 깨어나더라도 SSH 감시기가 실제
  단조 시계의 마감 시각을 다시 확인하도록 보완.
- 창 종료를 위해 예약한 Qt 콜백을 창 수명에 연결하고 daemon 작업의 늦은
  완료 신호가 Qt 종료와 겹쳐도 처리되지 않은 예외를 남기지 않도록 보완.
- 백그라운드 저장소 대조와 상태 확인이 겹쳐 SQLite WAL이 정상 삭제되는
  순간을 `DB_WRITE_FAILED`로 오인하지 않도록 파일 상태 확인을 단일화.
- 두 가상 soak의 유한 제한 시간을 20,000회 야간 범위와 workflow 전체 제한에
  맞추고, 느린 GitHub Windows 임시 디스크에서도 정상 작업을 오판하지 않도록 보완.

## 0.5.0 - 2026-08-30

- HTML 결과 보고서에서 패킷·바이트와 증감 영역을 제거하고, 최신 세션 결과와
  전체 추적 이력을 프로토콜 및 출발지·목적지 `IP:포트` 중심으로 단순화.
- 패킷·바이트 원본은 SQLite, CSV와 Raw에 계속 보존하며 기존 내보내기
  인터페이스와 저장 형식을 변경하지 않음.
- 비식별 Aruba tech-support 텍스트를 제한된 크기로 읽는 오프라인 파서와
  프로토콜·포트·Flags·ToS의 추측 없는 분석 코어를 추가하고 live
  `SessionObservation`/DB에 자동 합치지 않는 안전 경계를 유지.
- SSH 연결 수립·prompt·enable·명령 수신에 4 MiB·50,000행 상한을 적용하고,
  전체 poll 300초 deadline 또는 취소 시 socket-first abort로 blocked read를
  깨운 뒤 caller 경로에서 transport를 정리하도록 강화.
- Primary/Standby 및 여러 MD에 남은 poll 시간을 공정하게 배분하고 전수조회
  시작 MD를 순환하며, 활성 흐름 20,000개 포화 시 비권위 결과로 처리해
  잘못된 `MISS/CLOSED`가 발생하지 않도록 보완. 같은 poll에서 합법적으로
  여러 상태 전환이 발생해도 별도 수명주기 이벤트 상한 안에서 저장을 반복
  실패하지 않도록 관측 상한과 이벤트 상한을 분리.
- 매 poll 전체 Raw 32 MiB·관측 20,000건 상한을 수집기와 저장소 양쪽에서
  검증하고, 여러 command Raw를 section 길이·SHA-256이 포함된 단일 poll
  bundle로 저장해 장시간 실행의 파일 수 증가를 완화.
- Raw/DB poll 기록, 관리·외부 CSV/HTML 내보내기와 삭제를 manifest 기반으로
  복구하며 파일 hash·fsync·이동을 SQLite writer transaction 밖에서 끝내
  모니터링 기록과의 잠금 경합을 줄임.
- Windows 8.3 짧은 경로와 긴 경로가 섞여도 디렉터리 identity로 같은 관리
  루트를 확인하고, 양방향 별칭 아래의 junction/reparse 구성 요소는 계속
  거부하도록 보완. 종료 시 lease handle을 먼저 닫고 일시적 공유 위반은
  제한적으로 재시도하며 불완전 종료 경로는 worker lock을 기다리지 않음.
- HTML/CSV 행을 cursor로 스트리밍하고 Raw 무결성 hash에 chunk별 취소·진행
  확인을 추가해 대용량 보고서가 GUI 종료와 WAL 정리를 장시간 막지 않도록 함.
- 저장소 상태의 빠른 DB 집계와 명시적 background 파일 대조를 분리하고,
  외부 드라이브 부재로 미뤄진 보고서 복구는 기록 새로 고침에서 재시도하며
  남은 건수를 민감 경로 없이 안내.
- 조회·기록·내보내기·삭제를 daemon 작업으로 분리하고 15초 종료 grace 후
  다음 실행 복구로 전환하며, 시작 초기화는 반응형 화면과 30초 grace를 사용.
- 사용자 세션당 단일 실행 mutex, 절전 복귀·네트워크 변경 감지, MM 위치
  cache 무효화와 실행 중 자격증명 변경 감지를 추가.
- 예외 메시지·traceback·경로·운영 데이터를 제외한 1 MiB 순환 crash journal과
  비밀정보를 보존하지 않는 monitor failure DTO를 추가.
- GitHub Actions를 검증한 immutable Node 24 action commit으로 갱신하고,
  명령 생성→가짜 SSH→실제 파서→수명주기→SQLite/Raw 전체 파이프라인
  2,000회 PR soak 및 최대 50,000회 야간 경로를 추가.
- Windows 패키지 검증에서 Python 없는 PATH, 한국어 LocalAppData/보고서 경로,
  loopback fake SSH 정상·인증 실패, 실제 MM/MD 명령·파서·SQLite 저장을 확인.
- 실제 Aruba 장비와 회사 네트워크에는 접속하지 않았으며 자동·가상·패키지
  검증을 현장 호환성 증거로 확대 해석하지 않음.

## 0.4.1 - 2026-08-30

- PySide6 운영 화면에 일관된 운영 콘솔 테마를 적용하고 Windows 고대비 모드를
  존중하도록 개선했으며, 색상 대비와 테마 적용을 자동 테스트로 검증.
- 화면 디자인 기준과 세션 조회 화면 명세를 저장소의 디자인 시스템 문서로
  추가해 구현 기준을 명확히 기록.
- HTML을 사용 설명서나 진단 문서가 아닌 세션 추적 결과 보고서로 단순화.
- 보고서 상단에 KST 기준 추적 시간, 조회 조건, 전체 관측 수, 고유 세션 수와
  수집 상태를 표시하고 최신 세션 결과는 50건으로 제한.
- 세션별 첫 확인값과 마지막 확인값을 바탕으로 패킷·바이트 변화와 장비
  변경을 실제 저장값 그대로 표시하며, 감소 원인은 추측하지 않음.
- 전체 추적 이력은 기본으로 접어 두되 SQLite에 저장된 모든 관측 행을
  빠짐없이 포함하고 화면의 2,000행 제한은 적용하지 않음.
- HTML에서 진단 이벤트, 오류 코드, Raw 본문·경로·해시, CLI, 프로그램 흐름,
  Troubleshooting과 개발자 정보를 제외하고 자격증명·로그 미포함 원칙 유지.
- 이번 보고서 변경은 HTML 렌더링만 바꾸며 기존 설정, SQLite 스키마, CSV와
  Raw 저장 형식, 내보내기 인터페이스와 과거 실행 기록의 호환성 유지.
- 실제 Aruba 장비에는 접속하지 않고 비식별 fixture, 로컬 가짜 구현, 임시
  저장소, 대용량 이력과 Windows 패키지 검증으로 릴리스 범위를 제한.

## 0.4.0 - 2026-08-29

- 자주 쓰는 자격증명과 Source/Destination만 기본 표시하고
  `지속 모니터링 시작`·`현재 조회`, `고급 조건 보기/숨기기`,
  `상세 정보 보기/숨기기`로 입력과 결과를 점진적으로 펼치는 화면 제공.
- 운영 상태를 `대기·조회 중·정상·재시도 중·확인 필요` 다섯 값으로 제한.
- F12 UI Inspector를 한글 중심 `화면 개선 도우미`로 정리하고 기존 60개
  고정 ID를 유지한 채 상세 열 전환 ID를 추가해 총 61개, 세션 조회 24개
  카탈로그로 갱신.
- Primary MM 실패 뒤 Standby 성공을 복구된 경고로 처리하고 일시 MM/MD
  장애에 제한된 backoff를 적용해 비권위 실패 중 false CLOSED를 방지.
- 전체 MD 조회 빈도, poll 시간·Raw·관측 상한과 활성 SSH 취소 경로를 추가.
- 저장 공간 상태, 단계형 내보내기 복구, 삭제 미리보기 폐기·상한과 날짜·시간
  Raw 분산 저장을 추가하면서 기존 DB·보고서·Raw 경로 호환성 유지.
- 매 poll SSH 전에 빠른 1 GiB hard-stop 검사를 수행하고 전체 저장소 통계는
  60초 간격으로 제한했으며, UI 2,000행과 무관하게 HTML·CSV·SQLite에 전체
  관측 결과를 유지.
- 기록·내보내기·삭제와 대량 결과 렌더링이 GUI 이벤트 처리를 장시간 막지
  않도록 갱신 시점과 작업 수명주기를 분리.
- GitHub 공개 자산을 Windows x64 ZIP 하나로 단순화하고 SHA-256은 릴리스
  본문에, CycloneDX SBOM은 ZIP 내부에 제공.
- `continuous` 초안을 릴리스 ID로 재개하고 빈 중복 초안만 안전하게 정리하며,
  실패 시 이전 워크플로 커밋으로 되돌리지 않는 전진 복구 방식으로 보완.

## 0.3.1 - 2026-08-28

- 조회·모니터링·CSV·HTML·F12 Inspector의 기존 기능과 형식을 유지하면서
  실행 소유권, 취소, 종료 저장과 Qt 비동기 작업 수명주기를 강화.
- 한 번의 조회 결과, Raw 파일, 관측·진단·수명주기 이벤트를 복구 가능한
  단일 batch로 기록하고 프로세스 간 실행 lease로 동시 기록 충돌을 차단.
- Raw와 관리 내보내기에 작업 manifest, 무결성 확인, 장애 복구를 적용하고
  삭제 미리보기의 파일 SHA-256을 재확인하도록 강화.
- 설정, known_hosts, DB, Raw, 내보내기 관리 경로의 link/reparse point와
  실행 중 디렉터리 교체를 차단하고 설정·known_hosts 읽기 크기를 제한.
- CSV를 일정 크기 묶음으로 읽고 Raw 해시를 청크 단위로 계산해 긴 실행의
  메모리 사용량을 제한.
- 전역 line coverage 83%와 핵심 모듈 branch coverage 65%를 CI 정책으로
  추가하고 10,000회 모니터 fixture 및 500회 Qt 수명주기 soak를 검증.
- 릴리스 draft, GitHub 자산 digest, 인증·공개 재다운로드와 이동형
  `continuous` 실패 복구 절차를 강화.

## 0.3.0 - 2026-08-28

- 평소에는 숨겨져 있고 수정키 없는 `F12`로만 켜고 끄는 UI Inspector 추가.
- 세션 조회, 장비 설정, 기록 및 내보내기 화면의 운영 요소를 정적 한국어
  이름, 고정 ID, 화면 경로, 저장소 상대 소스 경로와 용도로 식별.
- 요소 선택 중인 클릭을 실제 동작에 전달하지 않고 `Esc`로 선택만 취소하는
  안전한 점검 흐름 제공.
- 클립보드 복사를 정적 UI 메타데이터와 비어 있는 현상·변경 요청 입력란으로
  제한하고 런타임 네트워크·자격증명·장비·표·Raw·로그·설정 값은 읽지 않음.
- Inspector 활성 상태를 CLI, 환경변수, 설정, 레지스트리 또는 SQLite에
  저장하지 않으며 외부 전송 기능을 제공하지 않음.

## 0.2.0 - 2026-08-28

- 기존 CSV 내보내기를 유지하면서 실행별 독립형 HTML5 결과 보고서 추가.
- 반응형 Summary, 조회 흐름, 세션/Flags, 수명주기, 진단, 수집 증거,
  Troubleshooting, Warning과 Quick Reference 제공.
- 외부 리소스 차단, 저장 데이터 HTML 이스케이프, Raw 본문 제외와
  생성 보고서의 Git/릴리스 패키지 유입 차단.

## 0.1.0 - 2026-08-28

- Windows 11용 한국어 Aruba Session Tracker 최초 사전릴리스.
- MM Current switch 탐색과 해당 MD의 필터형 datapath 세션 조회.
- 1회 조회, 지속 모니터링, SQLite/Raw/CSV 기록, 엄격한 SSH 호스트 키 검증.

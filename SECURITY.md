# 보안 정책

이 프로그램은 읽기 전용 Aruba 조회 도구입니다. 실제 장비 IP, 계정,
암호, 세션 원문, SQLite 파일 또는 고객 데이터를 공개 이슈에 첨부하지
마십시오. 문제를 보고할 때는 민감정보를 제거한 오류 코드와 단계만
공유하십시오.

HTML 결과 보고서도 내부 IP, 장비명과 세션 흐름을 포함하는 민감한 로컬
파일입니다. 보고서는 외부 CSS, JavaScript, 이미지 또는 CDN을 요청하지
않으며 DB와 장비에서 온 문자열은 HTML 이스케이프 후 출력합니다. UI의
2,000행 표시 제한과 무관하게 저장된 관측 행 전체가 포함될 수 있습니다.
진단 이벤트, 오류 코드, Raw CLI 본문·경로·해시, CLI 안내, 자격증명과 로그는
포함하지 않으며 패킷·바이트 수치도 HTML에서는 생략합니다. 해당 원본 수치는
SQLite, CSV와 Raw에 그대로 보존됩니다. 그래도 외부에 공유하기 전에는 파일
원문에서 IP, 장비명과 세션 정보를 직접 제거하고 검토하십시오.

호스트 키 변경, 명령 allowlist 우회, 자격증명 저장 또는 설정 변경 명령
실행 가능성을 발견하면 공개 이슈 대신 저장소 소유자에게 비공개로
알려주십시오.

## 로컬 관리 경로

설정, `known_hosts`, SQLite, Raw와 관리 내보내기 경로는 symbolic link와
Windows reparse point뿐 아니라 hardlink도 허용하지 않습니다. 프로그램이 실행 중 관리
디렉터리가 다른 항목으로 교체되거나 파일 크기·SHA-256이 저장된 값과
달라지면 내보내기와 삭제를 중단합니다. `.operations`의 manifest와 lease는
중단된 로컬 작업 복구에만 사용하며 자격증명이나 Raw CLI 본문을 저장하지
않습니다. lease를 열 때도 생성·열기 전후의 파일 identity와 link 수를
재확인해 다른 로컬 파일을 대신 수정하지 않도록 합니다. 이 폴더도 임의로
편집하거나 외부에 공유하지 마십시오.

사용자가 선택한 외부 CSV/HTML 경로도 기존 디렉터리의 일반 파일만 허용하고,
관리 저장소와 겹치는 경로, link/reparse point, Windows 장치명·ADS 별칭은
거부합니다. 외부 내보내기 복구 기록은 독립 소유권 증표와 SQLite 완료
증표를 교차 확인하며 자격증명이나 보고서 본문은 저장하지 않습니다.
외부 USB 드라이브가 분리되거나 UNC 공유를 사용할 수 없어도 앱은 정상적으로
시작하고 해당 외부 보고서 복구만 보류합니다. 저장 위치를 다시 사용할 수
있게 되기 전에는 대상 파일을 변경하지 않으며, 재연결 뒤 사용자가 기록을
새로 고칠 때 다시 검증하고 복구합니다.

## F12 화면 개선 도우미와 클립보드

화면 개선 도우미는 실행할 때마다 OFF로 시작하며 정적 UI 카탈로그만
사용합니다. 선택 결과와 클립보드에는 프로그램 버전, 정적 한국어 이름,
고정 ID, 화면 경로, 저장소 상대 소스 경로, 용도와 비어 있는 현상·변경
입력란만 포함할 수 있습니다. 현재 IP·호스트명·계정·암호·Enable 암호,
장비 정보, 표의 행·셀, Raw CLI, 진단·로그, 설정 값 또는 로컬 절대 경로를
읽거나 클립보드에 넣어서는 안 됩니다.

화면에서 항목을 선택하는 동안에는 마우스뿐 아니라 운영 위젯의 키보드
활성화와 단축키도 차단해야 합니다. `F12`, `Esc`와 도우미 자체의 조작만
허용합니다.

도우미는 클립보드 내용을 외부로 전송하지 않고 활성 상태를 CLI,
환경변수, 설정 파일, Windows 레지스트리 또는 SQLite에 저장하지 않습니다.
다만 복사한 정적 요청 양식에 사용자가 민감정보를 직접 적어 넣으면 해당
내용은 운영체제 클립보드와 사용자가 저장·전송한 문서에 남을 수 있습니다.
외부 공유 전에는 사용자가 추가한 내용을 다시 검토하고 민감정보를
제거하십시오.

## 저장 공간 보호

프로그램은 관리 DB, WAL, Raw와 내보내기 파일의 크기와 저장 볼륨 여유
공간을 로컬에서만 계산합니다. 매 poll의 SSH 실행 전에 빠른 여유 공간
검사를 수행하며 1 GiB 미만이면 네트워크 조회 자체를 시작하지 않습니다.
전체 관리 파일 통계는 최대 60초 간격으로 계산합니다. 공간 확보를 위한
자동 삭제는 하지 않으며, 삭제는 미리보기와 사용자의 명시적 확인을 거치는
기존 경로로만 수행합니다.

SQLite가 마지막 연결 종료 시 임시 WAL을 정상 삭제할 수 있으므로 크기 확인과
동시에 파일이 사라지면 WAL 크기 0으로 처리합니다. 존재하는 파일은 한 번의
상태 확인에서 일반 파일과 link·reparse 여부를 함께 검사하며 안전하지 않은
경로를 허용하는 방식으로 이 경합을 우회하지 않습니다.

## 장애 기록과 종료 복구

로컬 crash journal은 프로그램 버전, 발생 단계, 예외 클래스와 임의 incident
ID만 최대 1 MiB 순환 파일에 기록합니다. 예외 메시지, traceback, 로컬 경로,
IP, 계정, 암호, 장비 출력과 표 데이터는 받거나 저장하지 않습니다. journal과
상태 파일도 관리 경로의 link·reparse point·hardlink 검증을 통과해야 합니다.

조회·내보내기·삭제와 저장소 대조는 GUI와 분리된 취소 가능한 작업에서
실행합니다. 종료 유예 시간 안에 정리가 끝나지 않으면 트랜잭션을 강제로
중단하지 않고 창을 닫으며, 남은 manifest는 다음 실행에서 재검증한 뒤
복구합니다. 복구가 끝나기 전에는 결과 파일이 완료됐다고 표시하지 않습니다.

## 릴리스 검증 경계

버전 릴리스와 이동형 `continuous` 게시 작업은 고정된 runtime lock을 먼저
설치하고 `pip check`를 통과한 환경에서 ZIP을 다시 검증해야 합니다. 공개 전후
다운로드 바이트와 SHA-256을 비교하고 공개 자산은 Windows x64 ZIP 하나만
허용합니다. 게시 검증 의존성이 없거나 tag, commit, 최신 `main` 중 하나라도
다르면 공개하지 않습니다. 버전 릴리스는 exact annotated-tag commit에서
20,000-poll fixture-only soak도 별도 제한 job으로 통과해야 게시할 수
있습니다. 이 검증은 실제 Aruba 장비 호환성, 실제 장시간 운용이나
Authenticode 서명을 대신하지 않습니다.

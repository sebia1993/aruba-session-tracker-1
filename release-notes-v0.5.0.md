# Aruba Session Tracker v0.5.0 사전릴리스

v0.5.0은 기존의 단순한 조회 화면과 결과 형식을 유지하면서, 장시간 모니터링,
SSH 중단, 저장·내보내기·삭제, 시작과 종료의 실패 경계를 강화한 안정성
릴리스입니다. HTML은 프로토콜과 출발지·목적지 `IP:포트` 중심의 결과만
보여주며 패킷·바이트 원본은 SQLite, CSV와 Raw에 그대로 보존합니다.

## 장시간 조회 안정성

- SSH 연결, prompt, Enable과 명령 수신을 단일 300초 poll deadline 안에서
  제한하고, 취소나 시간 초과 시 socket을 먼저 닫아 멈춘 read를 깨웁니다.
- 연결부터 명령 완료까지 4 MiB·50,000행, poll 전체 Raw bundle은 최종
  직렬화 크기 32 MiB, 관측은 20,000건으로 제한합니다.
- Primary·Standby MM과 여러 MD에 남은 시간을 공정하게 배분하고 전체 MD
  조회 시작점을 순환합니다. 결과가 비권위이거나 상한에 닿으면 기존 세션을
  잘못 `MISS/CLOSED`로 바꾸지 않습니다.
- 절전 복귀와 네트워크 어댑터 변경 뒤에는 진행 중인 조회를 취소하고 MM 위치를
  다시 확인합니다. 같은 사용자 세션에서는 프로그램 한 개만 실행됩니다.

## 저장, 내보내기와 복구

- 여러 명령의 Raw를 section 길이와 SHA-256을 가진 poll 단위 묶음 하나로
  저장하면서 기존 Raw 파일과 과거 경로를 계속 읽습니다.
- Raw 검증과 HTML/CSV 행 읽기는 일정 크기로 나누고, 파일 hash·설치·격리를
  SQLite writer transaction 밖에서 완료해 장시간 DB 잠금을 줄입니다.
- 관리·외부 내보내기, Raw 기록과 삭제는 단계별 manifest로 복구합니다.
  외부 드라이브가 없으면 경로를 노출하지 않고 대기 건수만 안내하며, 기록
  새로 고침 때 다시 확인합니다.
- 관리 파일과 crash journal, manifest, lease는 symbolic link, Windows
  reparse point, hardlink 및 열기 전후 identity 변경을 거부합니다.
- Windows 8.3 짧은 경로와 긴 경로가 함께 사용되는 환경에서도 관리 루트의
  directory identity를 확인하고, 어느 별칭 방향이든 중간 junction은
  거부합니다. lease handle은 종료 전에 닫고 일시적 공유 위반만 제한적으로
  재시도합니다.
- 자동 삭제는 하지 않습니다. 저장 공간 5 GiB 미만은 경고하고 1 GiB 미만은
  다음 SSH 조회 전에 안전하게 중단합니다.

## 시작, 종료와 화면 응답성

- DB와 중단 작업 초기화는 최대 30초의 시작 화면에서 수행하고, 대용량 저장소
  대조는 주 창이 열린 뒤 백그라운드에서 이어갑니다.
- 조회, 기록표, 내보내기, 삭제와 종료 정리를 GUI와 분리합니다. 종료 유예
  시간 안에 끝나지 않은 로컬 작업은 강제로 끊지 않고 다음 실행에서 복구합니다.
- crash journal은 예외 메시지, traceback, 경로와 운영 데이터를 저장하지
  않고 단계, 예외 클래스와 incident ID만 제한된 크기로 기록합니다.
- HTML 결과 보고서는 최신 세션 50건과 접힌 전체 이력을 프로토콜 및
  출발지·목적지 `IP:포트` 중심으로 표시합니다. 진단, Raw, 자격증명, 로그와
  패킷·바이트 수치는 HTML에 넣지 않습니다.

## 오프라인 분석 코어

- 비식별 Aruba tech-support 텍스트를 제한된 크기로 읽고 프로토콜, 포트,
  Flags와 ToS를 추측 없이 해석하는 오프라인 분석 코어를 포함합니다.
- 이 결과는 운영 조회 결과나 기존 SQLite 기록에 자동으로 합치지 않습니다.
  실제 장비 데이터와 연결하는 사용자 UI는 이번 릴리스 범위가 아닙니다.

## 검증 범위와 한계

- 비식별 parser fixture, 수집기 fake, 127.0.0.1 loopback SSH 서버, 임시
  SQLite/Raw 저장소, 장애 주입, 강제 프로세스 종료 복구와 가상 장시간 조회로
  정상·실패 경로를 확인합니다.
- Windows x64 패키지는 Python 없는 `PATH`, 일반 사용자용 LocalAppData,
  한국어 경로, EXE GUI/HTML smoke 및 가짜 MM/MD SSH 정상·인증 실패 경로를
  검사합니다.
- 실제 Aruba 장비나 회사 네트워크에는 접속하지 않았습니다. 자동·가상·패키지
  결과는 현장 호환성, 실제 장시간 운용 또는 Authenticode 서명을 증명하지
  않습니다.
- 공개 자산은 unsigned Windows 11 x64 ZIP 하나입니다. SHA-256은 릴리스
  본문에 추가되고 CycloneDX SBOM은 ZIP 내부에 포함됩니다.

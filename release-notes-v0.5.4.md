# Aruba Session Tracker v0.5.4 사전릴리스

v0.5.4는 v0.5.3의 읽기 전용 조회, 장시간 안정성 경계와 기존
설정·SQLite·CSV·Raw 형식을 유지하면서 HTML 결과 보고서에 로컬 자동완성
필터를 추가하고 프로그램과 보고서의 정보 순서를 더 단순하게 정돈합니다.

## 변경 사항

- 프로그램은 기존 PySide6 위젯과 기능을 그대로 사용하면서 선택된 탭,
  조회·자격증명·고급·장비 영역, 주요·보조·중지 버튼과 결과표의 시각적
  우선순위를 분명하게 구분합니다. 키보드 포커스와 스크롤바 대비를 강화하고
  Windows 고대비 환경에서는 시스템 팔레트를 사용합니다.
- HTML 상단에는 조회 출발지 `IP:포트`와 목적지 `IP:포트`, 양방향 `↔` 또는
  단방향 `→`, 한글 수집 상태와 핵심 건수만 먼저 표시합니다. 최신 세션과
  동일한 최대 50개 기준의 프로토콜·장비 분포를 최대 5개씩 제공하되,
  필터된 결과를 인쇄할 때는 고정 분포를 숨깁니다.
- 결과 요약 아래에서 IP나 포트 일부를 입력하면 전체 추적 이력에 실제로
  존재하는 완성값을 최대 12개 추천합니다. 완전 일치, 앞부분 일치, 중간 포함
  순으로 정렬하고 각 값이 출발지·목적지·양쪽 중 어디에서 관측됐는지 함께
  표시합니다.
- 추천값을 마우스·터치로 선택하거나 방향키와 Enter로 선택하면 정확한 값으로
  즉시 필터링합니다. 보고서에 존재하는 완성값은 직접 입력 후 Enter로도
  적용할 수 있지만 일부 입력이나 존재하지 않는 값은 적용하지 않습니다.
- IP는 출발지 또는 목적지 IP, 포트는 출발지 또는 목적지 포트와 정확히
  일치하며 프로토콜 조건과 AND로 결합합니다. 다른 조건이 적용된 상태에서는
  해당 결과에서 실제 선택할 수 있는 값만 추천합니다.
- 최신 결과와 전체 추적 이력에 현재 표시 건수와 전체 건수를 함께 보여주고,
  최신 50건에는 없지만 전체 이력에는 있는 결과를 구분합니다. 인쇄에도 현재
  필터 조건과 결과가 적용되며 `전체 초기화` 뒤에는 전체 이력을 인쇄합니다.
- 필터는 화면 표시만 바꾸며 저장된 관측 행은 단일 HTML 안에 모두 남습니다.
  필터 상태는 파일, URL 또는 브라우저 저장소에 기록하지 않아 보고서를 다시
  열면 항상 전체 결과로 시작합니다.

## 보안과 호환성

- 보고서는 외부 CSS·JavaScript·이미지·CDN·웹폰트를 요청하지 않습니다.
  정확한 SHA-256 CSP 해시로 승인한 단 하나의 내장 로컬 필터 스크립트만
  사용하고 인터넷 통신, 외부 라이브러리, 브라우저 저장소, URL 변경,
  클립보드와 `eval`은 사용하지 않습니다.
- DB와 장비에서 온 값은 HTML 이스케이프 후 데이터로만 처리합니다. CSP가
  스크립트를 차단하면 필터 UI를 숨기고 전체 정적 보고서를 그대로 읽을 수
  있습니다.
- 기존 `RunReportSnapshot`, HTML 렌더링·원자적 쓰기·스트리밍 내보내기,
  `SessionStore.export_run_html` 인터페이스와 SQLite·CSV·Raw 형식은
  변경하지 않습니다.

## 검증 범위와 배포

- 비식별 fixture, 2,005행 이상과 20,000행 이력, HTML 주입값, CSP와
  키보드 자동완성 동작을 자동 검사합니다. 실제 headless Edge에서 20,000행
  추천·필터·키보드·복원 초기화·DOM 비누적과 CSP 차단 fallback을 실행하고,
  Edge 인쇄와 100·125·150% 배율·작은 창·고대비는 로컬 시각 검사합니다.
- Windows x64 패키지는 Python 미설치 환경을 가정한 EXE smoke와 보고서 생성,
  ZIP 무결성 및 ZIP 내부 SBOM을 별도로 검사합니다.
- 실제 Aruba 장비나 회사 네트워크에는 접속하지 않습니다. 자동 테스트·가상
  장비·패키지 검사 결과는 현장 호환성, 실제 장시간 운용 또는 Authenticode
  서명을 증명하지 않습니다.
- 공개 자산은 코드 서명되지 않은 Windows 11 x64 ZIP 하나입니다. SHA-256은
  릴리스 본문에 기록하고 CycloneDX SBOM은 ZIP 내부에 포함합니다.

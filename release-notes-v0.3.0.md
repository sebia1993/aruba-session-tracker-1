# Aruba Session Tracker v0.3.0 사전릴리스

기존 Aruba 세션 조회·모니터링과 CSV·HTML 내보내기를 유지하면서, 화면
요소의 고정 식별 정보를 안전하게 확인할 수 있는 로컬 전용 F12 UI
Inspector를 추가했습니다.

## 새 기능

- 앱 시작 시 항상 꺼져 있고 수정키 없는 일반 `F12`로만 켜고 끄는 UI
  Inspector
- 공통 7개, 세션 조회 23개, 장비 설정 20개, 기록 및 내보내기 10개로
  구성된 정확히 60개 ID의 정적 UI 카탈로그
- 마우스 호버 테두리, 요소 목록, 선택 결과 상세 보기와 클립보드 복사
- 프로그램 버전, 정적 한국어 이름, 고정 UI ID, 화면 경로, 저장소 상대
  소스 경로와 용도 제공
- 복사된 요청 양식에 사용자가 작성할 수 있는 빈 `현재 현상`과
  `원하는 변경` 입력란 제공
- `Esc`로 요소 선택만 취소하고 개발자 모드는 유지하는 반복 점검 흐름

## 안전 경계

- 요소 선택 중인 클릭, 더블클릭과 컨텍스트 메뉴는 실제 조회, 모니터링,
  설정 저장, CSV·HTML 내보내기 또는 기록 삭제 동작으로 전달되지 않습니다.
- Inspector는 코드에 등록된 정적 메타데이터만 사용합니다. 현재 IP,
  호스트명, 계정, 암호, Enable 암호, 장비 정보, 표 행·셀, Raw CLI,
  진단·로그, 설정 값 또는 로컬 절대 경로를 읽거나 복사하지 않습니다.
- 클립보드 복사 외의 저장·전송 기능은 없습니다. 활성 상태를 CLI,
  환경변수, `config.json`, Windows 레지스트리 또는 SQLite에 남기지
  않습니다.
- 사용자가 복사한 요청 양식에 민감정보를 직접 추가한 경우에는 외부 공유
  전에 해당 내용을 직접 제거하고 검토해야 합니다.
- 기존 읽기 전용 명령 allowlist, 필터 강제, 자격증명 비저장, 엄격한 SSH
  호스트 키 검증과 CSV·HTML 독립 내보내기 정책은 그대로 유지됩니다.

## 검증과 한계

정적 카탈로그 계약, 단축키와 선택 이벤트 차단, 민감정보 canary,
클립보드 형식, GUI 종료 정리와 기존 운영 화면 회귀를 비식별 fixture 및
offscreen GUI 테스트로 확인합니다. Windows 패키지는 SHA-256 sidecar,
CycloneDX SBOM, `BUILD_INFO.json`과 EXE smoke를 함께 검증합니다.

이 증거는 실제 키보드의 F12/Fn Lock, 입력 포커스, 재시작 OFF, 클립보드,
100%·125%·150% 배율, 고대비 또는 다중 모니터에서의 현장 UI 확인을
대신하지 않습니다. 또한 실제 Aruba 7240XM/AOS 장비 연결, 사내 데이터,
장시간 운용, Authenticode 서명이나 조직 보안 제품의 허용을 증명하지
않습니다. v0.3.0은 unsigned Windows x64 사전릴리스이며 실장비 확인은
승인된 읽기 전용 절차로 별도 수행해야 합니다.

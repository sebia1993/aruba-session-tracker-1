# Aruba Session Tracker v0.5.7 사전릴리스

v0.5.7은 v0.5.6의 읽기 전용 명령 제한, 단일 IP 조회, MM 출력 해석과
exact-once 저장 경계를 유지하면서 Windows 운영 화면과 단일 HTML 결과
보고서의 정보 구조·접근성을 개선합니다.

## 변경 사항

- 상단 운영 막대에 제품명, 버전, 로컬 전용·읽기 전용 범위를 표시합니다.
  출발지와 목적지는 별도 흐름 카드로 구분하고 현재 양방향 설정을 가운데에
  명시해 한쪽 IP만 입력하는 조회 범위도 쉽게 확인할 수 있습니다.
- 결과 제목, 다음 행동을 설명하는 빈 상태와 실행 상태 막대를 추가합니다.
  결과표와 Raw·진단 패널은 기본 크기에서 위아래로 배치하고, 창 높이가
  760 logical pixel 미만이거나 고급 조건이 열리면 좌우로 전환해 지원 최소
  크기에서도 결과표가 접히지 않도록 합니다.
- 기록 화면은 실제 선택 행이 있을 때만 CSV·HTML·선택 삭제를 활성화하며,
  기록 없음, 불러오는 중, 읽기 실패를 서로 다른 상태로 안내합니다. 장비
  설정의 자격증명 비저장 안내는 전체 폭으로 표시합니다.
- 탭, 결과표, Raw·진단, MM/MD 입력과 기록 동작의 accessible name 및 키보드
  focus 표시를 보강합니다. 기존 위젯, 61개 Developer Inspector ID, 15열
  결과 순서와 명령·SSH·파서·SQLite·Raw·CSV 형식은 변경하지 않습니다.
- HTML 필터를 보조 표면으로 구분하고 최신·전체 결과 수를 각 섹션 제목 옆에
  배치합니다. 활성 필터에 맞춰 최신 결과 설명과 빈 상태를 갱신하고,
  361~520 px에서는 두 열, 360 px 이하에서는 한 열로 정리합니다. 보고서는
  계속 외부 리소스 없이 단일 파일과 CSP hash로 승인한 스크립트 하나만
  사용합니다.

## 검증 예정 범위와 배포

- 비식별 fixture와 로컬 Qt offscreen 검증으로 세션 흐름, 빈 상태, 기록 선택,
  최소 1080×680 및 100%·125%·150% 배율, HTML 정적 계약을 회귀 검증할
  예정입니다.
- GitHub-hosted Windows x64의 Python 3.13 전체 검증, Microsoft Edge 보고서
  상호작용, fixture-only 20,000 poll soak, Windows 패키지 빌드와 EXE smoke를
  사전릴리스 게시 전 수행할 예정입니다. 이 문서에는 아직 성공 결과, workflow
  실행 번호, 자산 크기나 SHA-256을 확정해 기록하지 않습니다.
- 실제 Aruba 장비나 회사 네트워크에는 접속하지 않습니다. 자동 검증은 실제
  7240XM/AOS 현장 출력의 모든 변형, 실제 Windows 11 clean PC 장시간 운용,
  물리 키보드와 다중 모니터, Authenticode 서명을 증명하지 않습니다.
- 게시 검증이 모두 성공한 경우 공개 자산은 코드 서명되지 않은 Windows 11 x64
  ZIP 하나이며, 실제 SHA-256은 생성된 자산을 독립적으로 확인한 뒤 릴리스
  본문에 기록합니다.

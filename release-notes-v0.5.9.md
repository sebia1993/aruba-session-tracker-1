# Aruba Session Tracker v0.5.9 continuous 변경 안내

v0.5.9는 지속 모니터링의 SSH 연결을 poll 사이에 재사용하고, 화면의 운영
상태와 세션 결과 의미를 더 짧고 분명하게 구분합니다. 읽기 전용 명령 제한,
세션 수명주기, SQLite schema와 CSV·HTML·Raw 저장 형식은 그대로 유지합니다.

## 변경 사항

- 모니터링 중 같은 장비의 인증된 SSH 연결을 다음 poll에도 재사용합니다.
  사용자가 중지하거나 연결이 끊기거나 장비 설정, 이번 실행의 자격증명 또는
  네트워크 상태가 바뀐 경우에만 기존 연결을 닫고 새로 연결합니다.
- 시간 또는 poll 횟수에 따른 주기적인 재인증은 하지 않습니다. 새 연결의
  `AUTH_FAILED`는 계속 자동 재시도나 Standby 우회 대상이 아닌 확인 필요
  오류입니다.
- 진단 목록과 오류창에 민감정보가 없는 `ASxx` 전달 코드를 표시하고, 실행
  이력의 `최근 전달 코드` 열에도 남깁니다. 기존 운영 상태 다섯 값은 유지합니다.
- 결과 표를 수명주기 의미의 `관측 상태`와 장비 Flags의 참고 풀이인
  `세션 특이사항`으로 나눕니다. Raw Flags 원문은 `상세 열 보기`에서만
  표시합니다.
- 일반 `validate.ps1`, CI와 `continuous`는 `pytest -m 'not soak'`만 실행합니다.
  fixture-only 20,000 poll nightly는 예약 실행을 없애고 수동
  `workflow_dispatch` 전용으로 유지합니다. 버전 릴리스의 exact-tag soak는
  별도 job으로 유지합니다.

## 호환성과 검증 범위

- SQLite schema와 CSV·HTML·Raw 저장 형식, 필터형 읽기 전용 명령 allowlist,
  인증 실패의 비재시도 정책은 변경하지 않습니다.
- 비식별 fixture 기반의 일반 non-soak 자동 테스트만 변경 검증 범위로
  사용합니다. 이 문서에는 20,000 poll 수동 soak 성공이나 Windows 패키지
  성공 결과를 기록하지 않습니다.
- 실제 Aruba 장비, 회사 네트워크 또는 실제 Windows 11 clean PC에는 접속하지
  않았습니다. 자동 테스트는 실제 SSH 장시간 운용, 장비별 출력 변형과 현장
  네트워크 동작을 증명하지 않습니다.
- 게시 검증이 모두 성공한 경우 이번 변경은 이동형 `continuous` 사전릴리스의
  코드 서명되지 않은 Windows 11 x64 ZIP 하나로 공개하며 실제 SHA-256은
  자산을 독립적으로 확인한 뒤 릴리스 본문에 기록합니다. 변경 불가 `v0.5.9`
  태그 릴리스는 별도로 요청한 20,000 poll 검증 없이는 게시하지 않습니다.

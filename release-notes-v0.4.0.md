# Aruba Session Tracker v0.4.0 사전릴리스

이번 버전은 세션 조회 화면을 처음 사용하는 사람도 이해하기 쉽게 정리하고,
장시간 모니터링 중 일시 장애·저장 공간 부족·대용량 결과·강제 종료를 더
안전하게 처리합니다. 읽기 전용 Aruba 명령 allowlist와 기존 설정·SQLite·CSV·
HTML·Raw 형식은 유지됩니다.

## 단순해진 화면

- 기본 화면에는 실행 자격증명과 Source/Destination만 먼저 표시하고 Enable
  암호, 포트와 양방향 조건은 `고급 조건 보기/숨기기`로 펼칩니다.
- `지속 모니터링 시작`을 주 동작, `현재 조회`를 보조 동작으로 표시하고,
  통계 열과 Raw·진단은 `상세 정보 보기/숨기기`로 필요할 때만 표시합니다.
- 상태는 `대기·조회 중·정상·재시도 중·확인 필요` 다섯 값만 사용합니다.
  화면은 최대 2,000행이지만 SQLite·CSV·HTML 보고서에는 전체 결과를
  보존합니다.
- F12 기능은 `화면 개선 도우미`로 바뀌며 한글 설명을 먼저 보여주고 UI ID,
  버전과 코드 위치는 접힌 기술 정보에서 확인합니다. 복사 내용은 여전히
  정적 카탈로그로 제한되며 자동으로 외부에 전송되지 않습니다. 카탈로그는
  총 61개이며 세션 조회 항목은 24개입니다.

## 장시간 안정성

- Primary MM 실패 뒤 Standby MM이 성공하면 복구된 경고로 처리해 모니터링을
  계속하고, 일시적인 MM/MD 장애는 세션 종료로 잘못 판정하지 않습니다.
- 네트워크 계열 일시 장애에는 최대 300초의 제한된 재시도를 적용하고 전체
  MD 검색도 MM 위치 갱신 주기당 한 번으로 제한합니다.
- poll별 시간, Raw 크기와 관측 수 상한을 적용하고 활성 SSH 취소 시 블로킹
  읽기를 안전하게 해제합니다.
- 매 poll의 SSH 전에 빠른 여유 공간 검사를 수행해 1 GiB 미만이면 안전
  중단하고, 전체 저장소 통계는 최대 60초 간격으로 계산해 5 GiB 미만을
  경고합니다. 자동 삭제는 하지 않습니다.
- 관리 폴더와 사용자가 선택한 외부 경로의 CSV/HTML 내보내기는 단계별 복구
  상태를 기록하고, 삭제 미리보기는 취소·만료·오류·완료 경로에서 즉시
  정리합니다.
- 새 Raw 파일은 날짜와 시간별 하위 폴더로 분산하며 기존 평면 경로도 계속
  읽고 내보내고 삭제할 수 있습니다.

## 검증 범위

- 비식별 parser fixture, 수집기 fake, 127.0.0.1 전용 Paramiko loopback,
  임시 SQLite/Raw 저장소, 장애 주입, offscreen Qt와 Windows x64 패키지
  smoke를 사용합니다.
- 내보내기는 각 복구 단계 직후 별도 프로세스를 종료하고 재시작하는 방식으로
  검증했습니다. 이 결과는 갑작스러운 전원 차단의 물리적 내구성 증거가
  아닙니다.
- offscreen 별도 프로세스에서 100/125/150% 배율과 결정적 고대비 팔레트를
  검사했습니다. 실제 Windows 표시 환경의 육안 검증은 별도 한계로 남습니다.
- 자동 테스트와 가속 soak는 실제 Aruba MM/MD 접속이나 현장 장비 검증을
  의미하지 않습니다. 실제 장비에서 발견한 문제는 민감정보를 제거한 증상과
  오류 코드를 바탕으로 별도 수정합니다.
- 공개 자산은 unsigned Windows 11 x64 ZIP 하나입니다. SHA-256은 GitHub
  릴리스 본문에 생성 시 자동 추가되고 CycloneDX SBOM은 ZIP 안에 포함됩니다.
